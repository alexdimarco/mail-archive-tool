# Adversarial review — archive fidelity and the F2/F3 surfaces

> As of 2026-09-03, over the tree at `3b4a191` (the fidelity slices a–d merged,
> the friction/product fixes merged). Method: the assurance-kit adversarial pass
> run as a workflow — four finders (outside aggressor via mail content; insider
> with write access to the archive directory; integrity/concurrency; the
> unattended job and the operator who reads its results later), each finding
> handed to a refutation-default skeptic with the built binary. 13 raw findings,
> 11 confirmed or partial, 2 refuted. Every confirmed finding is dispositioned
> below; the fixes landed in the closing-fixes merges named in the last column.

## Findings and disposition

| # | Lens | Sev. | Verdict | Finding (as confirmed) | Fix (as refined by the skeptic) | Disposition |
|---|---|---|---|---|---|---|
| AGG2-1 | outside | high | CONFIRMED | internal/export/exporter.go:117-120 disambiguates a Message-ID reuse by appending `"#" + fp` to the key ONCE and then re-Gets, but never re-checks that the record now found actually has Fingerprint==fp. Because `#` is a legal character inside an internet Message-ID, an attacker who fully controls mail content can pre-occupy… | Primary: at exporter.go:119 change `key += "#" + fp` to `key += keySeparator + fp` (the NUL byte). NUL cannot appear in any source-derived key component (Message-ID parsing yields only vchar/multibyte; the sha fallback is hex), so the qualified key can never… | slice X1 — eb37754 |
| INT-CC-1 | integrity | high | CONFIRMED | The store token — which names both the on-disk directory and the first component of every manifest/index key — is keyed by the raw input string, never a canonical path. In `internal/app/run.go:439` the token seed is `exp.Manifest.Token(path, store)` where `path` is the literal `-input` argument; `DiscoverInputs` even computes… | Direction is correct: canonicalize the source id at the single token-seed boundary. In DiscoverInputs store the canonical form of p instead of raw p — resolve `filepath.EvalSymlinks(abs)` (the path exists there, Stat already succeeded) with fallback to… | slice X1 — eb37754 |
| AGG2-2 | outside | medium | CONFIRMED | cmd/mailarchive/main.go runSearch prints attacker-controlled, mail-derived text to the terminal with no control-character stripping: `from := r.SenderName` (main.go:395) and `subject := r.Subject` (main.go:399) are printed raw at main.go:403-404, and the snippet at main.go:406 goes through index.safeSnippet… | At the plain `search` printer in cmd/mailarchive/main.go runSearch (the block at lines ~390-409), pass every mail-derived field through the project's existing control-scrub before printing. Add a small helper mirroring internal/app/report.go:101 /… | slice X3 — 58bec68 |
| INS2-1 | insider | medium | CONFIRMED | Manifest.Token returns a persisted token straight from the manifest map with NO re-sanitization: internal/state/manifest.go:169 `if tok, ok := m.Stores[sourceID]; ok { return tok }`. state.Load never validates the Stores values. The exporter then uses the token directly as a path segment: internal/export/exporter.go:144-145… | Defense in depth at the read boundary and the write boundary; the finding's suggested fix is sound and I tighten it: 1) internal/state/manifest.go Token(): before returning m.Stores[sourceID], require it to be a single safe segment: non-empty, not "." or… | slice X1 — eb37754 |
| INS2-2 | insider | medium | CONFIRMED | The last-run record .mailarchive-lastrun.json is writable by an insider in <out>. ReadLastRun (internal/state/lastrun.go:147) does a plain json.Unmarshal (line 156) and strips nothing, unlike the schedule descriptor which strips control chars from every Job element (internal/schedule/descriptor.go:95 `d.Job[i] =… | Two-part fix, mirroring the treatment the descriptor already received. 1) Clean at the read choke point. In internal/state/lastrun.go ReadLastRun, after json.Unmarshal, strip control characters from every r.Job[i] and from r.Error (and cheaply from… | slice X2 — 28fcc77 |
| UJ-A1 | unattended | medium | PARTIAL | R18 promises "every scheduled run is recorded and status reports it," and the design (F4/FC10) makes `verify` a first-class schedulable job. But `app.Verify` (internal/app/verify.go:100-174) and `app.Reindex` (internal/app/reindex.go:29-53) take the archive lock and never call `state.WriteLastRun`; the only writers are… | Do NOT reuse the shared last-run record for verify (the finding's primary suggestion): a verify "running"/"ok" record would reset status's staleness clock (health.go uses lr.Started vs the export cadence), zero the completeness counts, mask a prior export… | slice X2 — 28fcc77 |
| INS2-3 | insider | low | PARTIAL | The schedule descriptor lives in <out> and is attacker-writable. On repair, repairSpec builds the install Spec straight from the descriptor's Job: cmd/mailarchive-gui/main.go:590 `Args: append([]string(nil), d.Job...)` and only calls spec.Validate() (cmd/mailarchive-gui/main.go:596-598), which checks control characters/absolute… | Mirror the INS-7 treatment already applied to Name/WrapperPath: do NOT copy d.Job/d.Log in repairSpec. For a GUI schedule the args are fully derivable from the (sanitized) name — reconstruct `Args = []string{"-job", job.PathFor(name)}` and `Log =… | slice X4 — b788788 |
| INT-CC-2 | integrity | low | PARTIAL | When `Lock.StillHeld()` reports the lock was removed or replaced mid-run, the export does NOT stop writing — it performs a full state-write to the contended archive. In `internal/app/run.go` the checkpoint closure records the loss and returns without committing (lines 208-210), but the outer loop's recovery branch then calls… | Real gap worth closing; treat as low. Concrete steps: 1) On lock loss, abandon without writing shared state. In run.go:224 and graph.go:134, drop the commit()+finish() and return lockLost immediately (let the deferred recordRun mark the run failed). The… | slice X1 — eb37754 |
| UJ-A3 | unattended | low | PARTIAL | BACKUP-NEEDS-ATTENTION.txt is written/removed only inside `updateAttentionSidecar` (internal/state/lastrun.go:102-123), which is reached only through `WriteLastRun` (lastrun.go:65-96), which only export/graph call. The GUI desktop notification `notifyFailure` (cmd/mailarchive-gui/main.go:195-198) is invoked only from the export… | Do NOT simply route verify/reindex through the export last-run + sidecar path as proposed — that conflates two records answering different questions ("did the last export succeed?" vs "is the archive intact right now?"), and would let an attested verify… | slice X2 — 28fcc77 |
| UJ-A4 | unattended | low | PARTIAL | internal/health/health.go:170 warns when `filepath.Clean(jo) != filepath.Clean(in.Out)`, where `jo` is the scheduled job's -out (from the descriptor, jobOutOf, health.go:261-272) and `in.Out` is the status -out made absolute (cmd/mailarchive/status.go:33 via abspath). `filepath.Clean` is case-preserving, but macOS APFS and… | Real but low-severity; fixing the false positive is still worthwhile. Replace the exact `filepath.Clean(jo) != filepath.Clean(in.Out)` inequality with a same-directory test: when both paths exist, treat them equal via os.SameFile(Stat(jo), Stat(in.Out)) (this… | slice X2 — 28fcc77 |
| UJ-A5 | unattended | low | CONFIRMED | JSONLastRun.Finished (internal/health/health.go:432) is a plain `time.Time` with no `omitempty` and is populated unconditionally from lr.Finished (health.go:472-479) whenever the last-run record is present — including a `running` record, whose Finished is the zero time (recordRun has not run). encoding/json then serializes… | Do NOT use `omitempty` (it would break MA-115 / b1_test.go:189-203, which requires `finished` to always be present in the fixed-shape last_run object). Instead make JSONLastRun.Finished a `*time.Time` (keep `json:"finished"`, no omitempty) and set it only for… | slice X2 — 28fcc77 |

## Refuted

- **INS2-4** — verify inspect->open is check-then-use: a component swapped to a symlink after the per-component Lstat is followed on read/baseline: The code mechanism the finding describes is real, but the actor it requires is a conceded non-goal, so no claimed property is broken. MECHANISM CONFIRMED (real check-then-use gap): - internal/app/verify.go inspect() resolves component-wise with Lstat and returns the resolved path `cur` as `full`: 335 fi, err := os.Lstat(cur) 339 if fi.Mode()&fs.ModeSymlink != 0 { return "symlink", 0, cur } 347 ret…
- **UJ-A2** — A backup blocked by the exclusive lock returns before beginRun and records nothing, so status shows the previous night's stale "ok" — a night with no backup looks healthy: The code mechanism is accurate and I reproduced it fully — but it is an explicitly conceded behaviour in docs/design-archive-fidelity.md §5, which per the review rules is REFUTED. CODE MECHANISM (accurate as claimed): - internal/app/run.go:113-116 acquires the lock and `return Result{}, err` on failure; the `defer recordRun(opts, beginRun(opts), &result, &err)` is only registered at run.go:121, AF…
