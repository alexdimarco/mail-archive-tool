# Build artifacts (anchored to the repo root so they do NOT match the
# cmd/mailarchive/ and cmd/mailarchive-gui/ source directories)
/bin/
*.exe
/mailarchive
/mailarchive-gui
/mailarchive-macos-universal

# Example / local export output
/export/
*.mailarchive-manifest.json
mailarchive.log
search.db
search.db-wal
search.db-shm

# OS / editor cruft
.DS_Store
*.swp

# Release artifacts
/dist/

# Work-in-progress handoff (not part of the project)
/continuation.md
